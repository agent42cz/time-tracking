# Use a flat node_modules layout (no .pnpm symlinks). This is required for
# Next.js standalone output: @vercel/nft traces dependencies through real
# paths, not symlinks, so Prisma's runtime binaries and similar native deps
# end up missing from the standalone bundle when pnpm uses its default
# symlink layout.
node-linker=hoisted
